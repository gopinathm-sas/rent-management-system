{
  "name": "my-new-project",
  "version": "1.0.0",
  "description": "",
  "main": "delete_collections.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest",
    "copy:web": "node scripts/copy-web-assets.js",
    "cap:sync": "npm run build && npx cap sync",
    "ios": "npm run build && npx cap sync ios && npx cap open ios",
    "deploy:live:hosting": "firebase deploy --project live --only hosting",
    "deploy:live:functions": "firebase deploy --project live --only functions",
    "deploy:live": "npm run build && firebase deploy --project live --only hosting,functions",
    "deploy:test:hosting": "firebase deploy --project test --only hosting",
    "deploy:test:functions": "firebase deploy --project test --only functions",
    "deploy:test": "npm run build && firebase deploy --project test --only hosting,functions"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^8.0.1",
    "@capacitor/app": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capgo/capacitor-native-biometric": "^8.0.3",
    "firebase": "^12.8.0",
    "firebase-admin": "^13.6.0",
    "lucide-react": "^0.303.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.21.1"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "@types/react": "^19.2.9",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.16",
    "firebase-tools": "^15.2.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "postcss": "^8.4.33",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.9.3",
    "vite": "^5.0.10"
  }
}
