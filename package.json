{
  "name": "my-new-project",
  "version": "1.0.0",
  "description": "",
  "main": "delete_collections.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "jest",
    "copy:web": "node scripts/copy-web-assets.js",
    "cap:sync": "npm run copy:web && npx cap sync",
    "ios": "npm run copy:web && npx cap sync ios && npx cap open ios",
    "deploy:live:hosting": "firebase deploy --project live --only hosting",
    "deploy:live:functions": "firebase deploy --project live --only functions",
    "deploy:live": "firebase deploy --project live --only hosting,functions",
    "deploy:test:hosting": "firebase deploy --project test --only hosting",
    "deploy:test:functions": "firebase deploy --project test --only functions",
    "deploy:test": "firebase deploy --project test --only hosting,functions"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gopinathm-sas/rent-management-system.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/gopinathm-sas/rent-management-system/issues"
  },
  "homepage": "https://github.com/gopinathm-sas/rent-management-system#readme",
  "dependencies": {
    "@capacitor-firebase/authentication": "^8.0.1",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capgo/capacitor-native-biometric": "^8.0.3",
    "firebase-admin": "^13.6.0"
  },
  "devDependencies": {
    "firebase-tools": "^15.2.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
